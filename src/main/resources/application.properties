spring.r2dbc.url=r2dbc:mysql://localhost:3306/test?sslMode=DISABLED
spring.r2dbc.username=root
spring.r2dbc.password=72505813
# Opciones del Pool (recomendado)
spring.r2dbc.pool.enabled=true
spring.r2dbc.pool.initial-size=5
spring.r2dbc.pool.max-size=20
spring.r2dbc.pool.max-idle-time=30m
# spring.r2dbc.pool.validation-query=SELECT 1 # Para MySQL, a veces no es necesario o puede dar warnings si el usuario no tiene ciertos permisos.
# Configuración de inicialización de base de datos
# Si quieres que schema.sql se ejecute:
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql
# spring.sql.init.continue-on-error=false
# Logging (ajusta según necesidad)
logging.level.org.springframework.r2dbc=DEBUG
logging.level.io.r2dbc.mysql=DEBUG
logging.level.io.r2dbc.pool=DEBUG
logging.level.org.springframework.transaction.reactive=DEBUG
logging.level.org.example.demo=DEBUG